apiVersion: apps/v1
kind: Deployment
metadata:
  name: digital-notary-redis
  namespace: digital-notary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digital-notary-redis
  template:
    metadata:
      labels:
        app: digital-notary-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: digital-notary-redis
  namespace: digital-notary
spec:
  selector:
    app: digital-notary-redis
  ports:
    - port: 6379
      targetPort: 6379
